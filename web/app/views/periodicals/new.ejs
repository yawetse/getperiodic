<div class="page-header">
    <h1>Create a new Volume</h1>
    <p class="lead">A Volume is simply a collection of feeds, by creating a volume, you're able to group feeds from different users under a shared name (for example for Glamour, there's a shared Volume called "Glamour" which has all of the editors and social accounts connected to in one place.</p>
</div>

<% form_for(periodical, {action: path_to.periodicals(), method: 'POST', id: "periodical_form", class: 'form-horizontal'}, function (form) { %>
    <%- partial('periodicals/form.ejs', {locals: {form: form, periodical: periodical}}) %>
    <div class="form-actions">
        <%- form.submit('<i class="icon-ok icon-white"></i> Create periodical', {class: 'btn btn-primary'}) %> or
        <%- link_to('Cancel', path_to.periodicals(), {class: 'btn'}) %>
    </div>
<% });%>
